---
# Preferred on RHEL control nodes:
#   dnf install -y ansible-collection-redhat-leapp rhel-system-roles
#
# Preferred with Automation Hub:
#   ansible-galaxy collection install infra.leapp redhat.rhel_system_roles
#
# Lab fallback (Galaxy upstream; bootstrap-collections.sh renames to infra.leapp):
collections:
  # - name: infra.leapp
  #   version: ">=1.7.0"

  - name: infra-leapp-1.7.6.tar.gz
    type: file
