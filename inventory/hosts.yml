---
all:
  children:
    leapp_targets:
      children:
        leapp_rhel8:
          hosts:
            leapp-rhel8:
              # ansible_host is set in inventory/lab.generated.yml by
              # scripts/inventory-from-libvirt.sh after the VM gets a DHCP lease.
              ansible_user: cloud-user
        leapp_rhel9:
          hosts:
            leapp-rhel9:
              ansible_user: cloud-user
