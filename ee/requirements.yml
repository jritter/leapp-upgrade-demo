---
# Collections installed into the custom LEAPP demo execution environment.
# Resolved from Automation Hub via ee/ansible.cfg during ansible-builder galaxy stage.
collections:
  - name: infra-leapp-1.7.6.tar.gz
    type: file
  - name: redhat.rhel_system_roles
